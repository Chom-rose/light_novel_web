<% if (user && user.id === novel.user_id) { %>
    <!-- ปุ่มแก้ไข / ลบ -->
    <div class="mt-4 flex gap-2">
      <button id="editNovelBtn" 
              class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">
        ✏️ แก้ไข
      </button>
      <button id="deleteNovelBtn" 
              class="px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700">
        🗑️ ลบ
      </button>
    </div>
  <% } %>
  
  <!-- Modal แก้ไข -->
  <div id="editModal" class="fixed inset-0 bg-black/50 hidden items-center justify-center z-50">
    <div class="bg-white rounded-xl p-6 shadow-lg max-w-md w-full">
      <h2 class="text-lg font-bold mb-4">แก้ไขนิยาย</h2>
      <form id="editNovelForm" class="space-y-3">
        <input type="text" id="editName" name="name"
               class="w-full border rounded p-2" placeholder="ชื่อเรื่อง" value="<%= novel.name %>">
        <input type="text" id="editAuthor" name="author"
               class="w-full border rounded p-2" placeholder="ผู้แต่ง" value="<%= novel.author %>">
        <input type="text" id="editCategory" name="category"
               class="w-full border rounded p-2" placeholder="หมวดหมู่" value="<%= novel.category %>">
        <textarea id="editDescription" name="description"
                  class="w-full border rounded p-2" rows="4" placeholder="คำโปรย"><%= novel.content %></textarea>
        <input type="text" id="editCoverImage" name="coverImage"
               class="w-full border rounded p-2" placeholder="ลิงก์รูปปก" value="<%= novel.image %>">
        <div class="flex justify-end gap-2">
          <button type="button" id="closeEditModal" class="px-3 py-1 border rounded">ยกเลิก</button>
          <button type="submit" class="px-3 py-1 bg-green-600 text-white rounded hover:bg-green-700">บันทึก</button>
        </div>
      </form>
    </div>
  </div>
  