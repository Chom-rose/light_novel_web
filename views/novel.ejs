<%- include('includes/head') %>
</head>
<body>
  <%- include('includes/nav') %>

  <main class="max-w-4xl mx-auto p-6">
    <!-- ข้อมูลนิยาย -->
    <div class="flex gap-6 mb-8">
      <img src="<%= novel.image %>" alt="<%= novel.name %>"
           class="w-40 h-56 object-cover rounded shadow">
      <div>
        <h1 class="text-3xl font-bold"><%= novel.name %></h1>
        <p class="text-gray-600">ผู้เขียน: <%= novel.author %></p>
        <p class="mt-2 text-gray-800"><%= novel.content %></p>
        <p class="mt-2 text-sm text-gray-500">หมวดหมู่: <%= novel.category %></p>
      </div>
    </div>

    <!-- สารบัญ -->
    <h2 class="text-xl font-bold mb-4">สารบัญ</h2>
    <ul class="space-y-2">
      <% novel.chapters.forEach(ch => { %>
        <li class="border-b pb-2">
          <% if (ch.is_premium === 1) { %>
            <% if (!user) { %>
              <!-- ยังไม่ล็อกอิน -->
              <span class="text-gray-400"><%= ch.title %> 🔒 (ล็อกอินเพื่ออ่าน)</span>
            <% } else if (user.is_premium === 1 || user.id === novel.user_id) { %>
              <!-- เป็นพรีเมียม หรือ เจ้าของเรื่อง -->
              <a href="/light-novel/api/novels/<%= novel.id %>/chapters/<%= ch.id %>"
                 class="text-blue-600 hover:underline">
                <%= ch.title %> 🔒
              </a>
            <% } else { %>
              <!-- เป็นสมาชิกฟรี -->
              <span class="text-gray-400"><%= ch.title %> 🔒 (สำหรับพรีเมียมเท่านั้น)</span>
              <a href="/premium"
                 class="ml-2 text-sm text-indigo-600 hover:underline">อัปเกรดเป็นพรีเมียม</a>
            <% } %>
          <% } else { %>
            <!-- ตอนฟรี -->
            <a href="/light-novel/api/novels/<%= novel.id %>/chapters/<%= ch.id %>"
               class="text-blue-600 hover:underline">
              <%= ch.title %>
            </a>
          <% } %>
        </li>
      <% }) %>
    </ul>
  </main>
</body>
</html>